package bridgePackage;

public interface Implementor {
	// the implementor defines an interface for its concrete
	// subclasses, describing the implementation of the abstract
	// classes in abstractions (optional)
	public void operationImp();
}
